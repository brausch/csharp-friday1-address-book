<!DOCTYPE html>
<html>
<head>
  <meta charset='utf-8'>
  <title>To do</title>
  <link rel='stylesheet' href='https://maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css'>
</head>
  <body>
    <h1>@Model.GetName()</h1>
    <h3>@Model.GetPhone()</h3>
    <h3>@Model.GetAddress1()</h3>
    <h3>@Model.GetAddress2()</h3>

    <p><a href="/add_contact">Add another Contact!</a></p>
    <a href='/contacts'>View all Contacts</a>
    </form>
  </body>
</html>
